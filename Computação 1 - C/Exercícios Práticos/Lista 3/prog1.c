/* Este programa recebe 2 números e retorna sua soma.
*/

#include <stdio.h>

int main (void){

    int a, b, x;

    scanf("%d %d", &a, &b);

    x = a + b;

    printf("X = %d\n", x);
    
    return 0;
}